{
  "name": "fluffy-cat-pages",
  "version": "3.0.0",
  "private": true,
  "description": "Six-category companion homepage with animated cat, editable journals, vision drafts and focus timer",
  "scripts": {
    "test": "node --test tests/core.test.cjs tests/home-core.test.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
